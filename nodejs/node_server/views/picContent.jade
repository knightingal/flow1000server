extends layout

block content
  h1= title
  p(style="position: fixed; top: 0px;") #{dirName}
  each pic in pics 
    p
      img(src="http://127.0.0.1:8081/picDirs/picRepository/" + picpage + "/" + pic)
