extends layout

block content
  h1= title
  p Welcome to #{title}
  each val in picList
    p
      a(href="http://127.0.0.1:8081/picDirs?picpage=" + val.index)= val.name
      img(src="http://127.0.0.1:8081/picDirs/picContent/" + val.index + "/" + "1.jpg", class="littleimg")
